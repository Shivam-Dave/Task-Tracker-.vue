// test/setup-env.ts
import * as pactum from 'pactum';

beforeAll(async () => {
  // Ensure your application is running before tests start
});

afterAll(async () => {
  // Close any resources if necessary
});

pactum.settings.setLogLevel('TRACE');
